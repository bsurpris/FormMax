// Stub for @mediapipe/pose – we use MoveNet (tfjs runtime), not BlazePose (mediapipe runtime).
// The pose-detection library imports this module unconditionally, so we provide an empty export.
export const Pose = {};
export const POSE_CONNECTIONS = [];
export const POSE_LANDMARKS = {};
export const POSE_LANDMARKS_LEFT = {};
export const POSE_LANDMARKS_RIGHT = {};
export const POSE_LANDMARKS_NEUTRAL = {};
export const VERSION = '0.0.0-stub';
export default {};
